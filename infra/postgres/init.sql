-- PostgreSQL init script
-- Runs once on first startup

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Indexes will be created by Alembic migrations.
-- This file just ensures the DB and extensions are ready.
